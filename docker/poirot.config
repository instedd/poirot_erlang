% Configuration for standalone Poirot receiver in Docker
[
  {poirot, [
    {modules, [
      zmq_receiver,
      {indexer, [
        {elasticsearch_url, "http://elasticsearch:9200/"}
      ]}
    ]}
  ]}
].
